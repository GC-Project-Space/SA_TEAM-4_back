spring:
  application:
    name: SA_project

  security:
    user:
      name: user
      password: password

  datasource:
    url: jdbc:mysql://localhost:3306/GAMAGOCHI
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  jwt
